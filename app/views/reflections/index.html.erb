<h1>Listing reflections</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Details</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reflections.each do |reflection| %>
  <tr>
    <td><%= reflection.title %></td>
    <td><%= reflection.details %></td>
    <td><%= link_to 'Show', reflection %></td>
    <td><%= link_to 'Edit', edit_conversation_reflection_path(reflection) %></td>
    <td><%= link_to 'Destroy', reflection, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Reflection', new_conversation_reflection_path(@conversation) %>
