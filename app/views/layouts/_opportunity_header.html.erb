<div class="feature-mast">
  <div class="wrapper">
    <div class="content-container">

      <%# The hope is that we can pull the image used in the project splash page as a background image for this section. It will give us the most flexiblity %>
      <div class="mod project-banner" style="background-image: url(http://s3.amazonaws.com/com.theciviccommons.int/images/flats/flats-banner.jpg);">
        <div class="project-banner-titlebar">
          <p class="project-banner-title">Flats Forward <a class="project-banner-link" href="#">Explore the whole project &#10145;</a></p>
        </div>
      </div>

      <div class="main-content">

        <div class='main-content-left'>
          <img src='<%= @conversation.image.url(:panel) %>' width='120' height='80' alt='<%= @conversation.title %>' class='featured main-img'\>
        </div>
        <div class='main-content-right'>
          <h1><%= @conversation.title %></h1>
          <p class="subtitle">Started on <%= @conversation.created_at.strftime('%b %d, %Y') %> by <%= text_profile(@conversation.owner) %></p>
          <%= render :partial => '/layouts/social_links', :locals => {conversation_title: @conversation.title} %>
        </div>

        <div class='convo-summary'>
          <p><%= raw(@conversation.summary) %></p>
        </div>

        <%= render :partial => '/layouts/opportunity_navigation', :locals => {
          conversation: @conversation,
          participants_count: @conversation.community_user_ids.count,
          actions_count: @conversation.actions.count,
          reflections_count: @conversation.reflections.count
        } %>

      </div><!-- /.main-content -->

      <div class='aside supplementary convo-mast-buttons'>

        <span id='subscription'>
          <%= render :partial => '/subscriptions/subscription', :locals => {:subscribable => @conversation} %>
        </span>

      </div><!-- /.aside -->

    </div>

  </div>
</div>
